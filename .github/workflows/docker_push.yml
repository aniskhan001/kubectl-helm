name: "Docker push"
on: [push]

jobs:
  build:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v1
      - name: Build the Docker image
        run: docker build . -t aniskhan001/kubectl-helm --build-arg K8S_VERSION=v1.16.2 --build-arg HELM_VERSION=v3.0.0
